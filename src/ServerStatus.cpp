#include "ServerStatus.hpp"

ServerStatus::ServerStatus(int serverSocket)
{
    this->_serverSocket = _serverSocket;
}
/* {
    this->_clients = std::unordered_map<int, std::string>();
    this->_channels = std::unordered_map<std::string, std::vector<int>>();
} */